## Summary

<!-- What does this PR change and why? -->

## Checklist

- [ ] `uv run ruff check .` / `uv run ruff format --check .` / `uv run mypy` / `uv run pytest -q` all pass
- [ ] Tests added or updated (aioresponses — no live API calls)
- [ ] PR title is conventional-commit style (it becomes the squash commit message)
